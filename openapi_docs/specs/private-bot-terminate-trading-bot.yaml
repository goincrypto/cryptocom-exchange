openapi: 3.0.3
info:
  title: private/bot/terminate-trading-bot
  version: 1.0.0
servers:
  - url: https://api.crypto.com/exchange/v1
    description: Production
  - url: https://uat-api.3ona.co/exchange/v1
    description: UAT Sandbox
paths:
  /private/bot/terminate-trading-bot:
    post:
      tags:
        - Trading Bot API
      x-apply-to:
        - rest
      summary: private/bot/terminate-trading-bot
      description: Terminates a Trading Bot. All open orders placed by the bot are cancelled.
      operationId: privateBotTerminateTradingBot
      x-codeSamples:
        - lang: JavaScript
          label: JavaScript
          source: |
            const ccxt = require('ccxt');
            const exchange = new ccxt.cryptocom({
              apiKey: 'YOUR_API_KEY',
              secret: 'YOUR_SECRET',
              enableRateLimit: true,
            });
            const res = await exchange.v1PrivatePostPrivateBotTerminateTradingBot({
              bot_id: 1,
              bot_type: 'DCA',
              close_position: false
            });
            console.log(res);
        - lang: Python
          label: Python
          source: |
            import ccxt
            exchange = ccxt.cryptocom({
                'apiKey': 'YOUR_API_KEY',
                'secret': 'YOUR_SECRET',
                'enableRateLimit': True,
            })
            res = exchange.v1_private_post_private_bot_terminate_trading_bot({
                'bot_id': 1,
                'bot_type': 'DCA',
                'close_position': False
            })
            print(res)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrivateBotTerminateTradingBotRequest"
            example:
              id: 1
              method: private/bot/terminate-trading-bot
              api_key: YOUR_API_KEY
              sig: DIGITAL_SIGNATURE
              nonce: 1610905028000
              params:
                bot_id: 1
                bot_type: DCA
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateBotTerminateTradingBotResponse"
              example:
                id: 1
                method: private/bot/terminate-trading-bot
                code: 0
        "400":
          description: Bad request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateBotTerminateTradingBotResponse"
              example:
                id: 1
                method: private/bot/terminate-trading-bot
                code: "40001"
                message: BAD_REQUEST
        "401":
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateBotTerminateTradingBotResponse"
              example:
                id: 1
                method: private/bot/terminate-trading-bot
                code: "40101"
                message: UNAUTHORIZED
        "408":
          description: Request timeout.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateBotTerminateTradingBotResponse"
              example:
                id: 1
                method: private/bot/terminate-trading-bot
                code: "40801"
                message: REQUEST_TIMEOUT
        "429":
          description: Too many requests.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateBotTerminateTradingBotResponse"
              example:
                id: 1
                method: private/bot/terminate-trading-bot
                code: "42901"
                message: TOO_MANY_REQUESTS
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateBotTerminateTradingBotResponse"
              example:
                id: 1
                method: private/bot/terminate-trading-bot
                code: "50001"
                message: INTERNAL_SERVER_ERROR
components:
  schemas:
    PrivateBotTerminateTradingBotRequest:
      type: object
      x-archetype: Request
      required:
        - id
        - method
        - api_key
        - sig
        - nonce
        - params
      description: Request body for private/bot/terminate-trading-bot.
      properties:
        id:
          type: string
          format: int64
          description: Request id (echoed in response)
        method:
          type: string
          description: Must be "private/bot/terminate-trading-bot"
          example: private/bot/terminate-trading-bot
        api_key:
          type: string
          description: Your API key (only required for private REST)
        sig:
          type: string
          description: HMAC-SHA256 signature in hex (only required for private REST)
        nonce:
          type: string
          format: int64
          description: Current timestamp in milliseconds, e.g. "1610905028000".
        params:
          type: object
          required:
            - bot_id
            - bot_type
          properties:
            bot_id:
              type: integer
              format: int64
              description: Trading Bot ID
              example: 1
            bot_type:
              $ref: "#/components/schemas/BotType"
            close_position:
              type: boolean
              description: For FUNDING_ARBITRAGE - whether to close open positions on termination
              example: false
    BotType:
      type: string
      enum:
        - DCA
        - TWAP
        - GRID
        - FUNDING_ARBITRAGE
      description: |
        Trading bot type:
        - DCA: Dollar Cost Averaging
        - TWAP: Time-Weighted Average Price
        - GRID: Grid trading
        - FUNDING_ARBITRAGE: Funding arbitrage
    PrivateBotTerminateTradingBotResponse:
      type: object
      x-archetype: Response
      description: Response for private/bot/terminate-trading-bot.
      properties:
        id:
          type: string
          format: int64
          description: Original request identifier
        method:
          type: string
          description: Method name
          example: private/bot/terminate-trading-bot
        code:
          type: string
          format: int32
          description: 0 for success; error code otherwise
        message:
          type: string
          description: Error message for non-zero codes
