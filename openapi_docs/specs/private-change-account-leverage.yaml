openapi: 3.0.3
info:
  title: private/change-account-leverage
  version: 1.0.0
servers:
  - url: https://api.crypto.com/exchange/v1
    description: Production
  - url: https://uat-api.3ona.co/exchange/v1
    description: UAT Sandbox
paths:
  /private/change-account-leverage:
    post:
      tags:
        - Trading
      x-apply-to:
        - rest
      summary: private/change-account-leverage
      description: Changes the maximum leverage used by the account. Please note, each instrument has its own maximum leverage. Whichever leverage (account or instrument) is lower will be used.
      operationId: privateChangeAccountLeverage
      x-codeSamples:
        - lang: JavaScript
          label: JavaScript
          source: |
            const ccxt = require('ccxt');
            const exchange = new ccxt.cryptocom({
              apiKey: 'YOUR_API_KEY',
              secret: 'YOUR_SECRET',
              enableRateLimit: true,
            });
            const res = await exchange.v1PrivatePostPrivateChangeAccountLeverage({
              account_id: '52e7c00f-1324-5a6z-bfgt-de445bde21a5',
              leverage: 10,
            });
            console.log(res);
        - lang: Python
          label: Python
          source: |
            import ccxt
            exchange = ccxt.cryptocom({
                'apiKey': 'YOUR_API_KEY',
                'secret': 'YOUR_SECRET',
                'enableRateLimit': True,
            })
            res = exchange.v1_private_post_private_change_account_leverage({
                'account_id': '52e7c00f-1324-5a6z-bfgt-de445bde21a5',
                'leverage': 10,
            })
            print(res)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrivateChangeAccountLeverageRequest"
            example:
              id: "1"
              method: private/change-account-leverage
              api_key: YOUR_API_KEY
              sig: DIGITAL_SIGNATURE
              nonce: "1611022832613"
              params:
                account_id: 52e7c00f-1324-5a6z-bfgt-de445bde21a5
                leverage: 10
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateChangeAccountLeverageResponse"
              example:
                id: "1"
                method: private/change-account-leverage
                code: "0"
        "400":
          description: Bad request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateChangeAccountLeverageResponse"
              example:
                id: "1"
                method: private/change-account-leverage
                code: "40001"
                message: BAD_REQUEST
        "401":
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateChangeAccountLeverageResponse"
              example:
                id: "1"
                method: private/change-account-leverage
                code: "40101"
                message: UNAUTHORIZED
        "408":
          description: Request timeout.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateChangeAccountLeverageResponse"
              example:
                id: "1"
                method: private/change-account-leverage
                code: "40801"
                message: REQUEST_TIMEOUT
        "429":
          description: Too many requests.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateChangeAccountLeverageResponse"
              example:
                id: "1"
                method: private/change-account-leverage
                code: "42901"
                message: TOO_MANY_REQUESTS
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateChangeAccountLeverageResponse"
              example:
                id: "1"
                method: private/change-account-leverage
                code: "50001"
                message: INTERNAL_SERVER_ERROR
components:
  schemas:
    PrivateChangeAccountLeverageRequest:
      type: object
      x-archetype: Request
      required:
        - id
        - method
        - api_key
        - sig
        - nonce
        - params
      description: Request body for private/change-account-leverage.
      properties:
        id:
          type: string
          format: int64
          description: Request id (echoed in response)
        method:
          type: string
          description: Must be "private/change-account-leverage"
          example: private/change-account-leverage
        api_key:
          type: string
          description: Your API key (only required for private REST)
        sig:
          type: string
          description: HMAC-SHA256 signature in hex (only required for private REST)
        nonce:
          type: string
          format: int64
          description: Current timestamp in milliseconds, e.g. "1771761038000".
        params:
          type: object
          required:
            - account_id
            - leverage
          properties:
            account_id:
              type: string
              description: Account ID to change the leverage. Must be the currently logged-in user's account.
            leverage:
              type: integer
              format: int32
              description: Maximum leverage for the account. Valid values 1-100 (inclusive).
    PrivateChangeAccountLeverageResponse:
      type: object
      description: |
        Response for private/change-account-leverage (HTTP 200/4xx/5xx).
        code === 0 → success, result present.
        code !== 0 → error, message and/or original present.
      properties:
        id:
          type: string
          format: int64
          description: Echoed request id
        method:
          type: string
          description: Method invoked
        code:
          type: string
          format: int32
          description: 0 = success; non-zero indicates error (reason code).
        result:
          type: object
          description: Present when code === 0; no result block per docs.
          properties: {}
        message:
          type: string
          description: Present when code !== 0.
        original:
          type: string
          description: Present when code !== 0.
